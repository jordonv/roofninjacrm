# DOMAIN (change when you move domains)
NEXT_PUBLIC_APP_URL=https://your-test-domain.example

# SUPABASE (safe to expose anon + url to browser; DO NOT expose service role on client)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE=your_service_role_key   # server-only (used in webhook route)

# STRIPE (server-only secrets)
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_PRICE_ID=price_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
